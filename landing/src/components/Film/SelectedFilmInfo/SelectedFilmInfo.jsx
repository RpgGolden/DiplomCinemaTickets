import styles from "./SelectedFilmInfo.module.scss"

function SelectedFilmInfo(props) {
    return (
        <div className={styles.SelectedFilmInfo}>
            
        </div>
    );
}

export default SelectedFilmInfo;